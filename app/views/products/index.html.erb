<p style="color: green"><%= notice %></p>
<nav class="navbar">
  <div class="container-fluid flex justify-content-center">
    <%= render partial: "search/form",locals:{results:[]}%>
  </div>
</nav>
  <div class="container">
<div id="products">
<% @products.each do |product| %>
  <%= render product %>
<% end %>
</div>
</div>
<div class="container">
  <div class="row justify-content-center mb-5 ">
    <div class="col-auto btn btn-secondary">
      <%= link_to "New product", new_product_path, class: "text-decoration-none text-dark" %>
    </div>
  </div>
</div>

